 <%= erb :_menu, :locals => { :current => 'playlist' } %>

<div class="page">
  <div class="songs">
    <div class="playlist">
      <div class="inner">

        this is where the playlist goes

        <ul>
            <% for item in data %>
            <li>
              <b><%= item['name'] %></b> by 
              <b><%= item['artist'] %></b> 
            </li>

          <% end %>
        <ul>

      </div>
      <span class="border"></span>
    </div>
  </div>
</div>